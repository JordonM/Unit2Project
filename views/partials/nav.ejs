<%- include('./header') %>

<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <div class="container-fluid">
        <a href="/" class="navbar-brand">DRIVEHIVE</a>
        <% if (user) {%>
            <a href="/drivers" class="nav-link text-warning me-3">
                Hello <%= user.name %>
            </a>
        <% } %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <% if (user) {%>
                <a href="/drivers" class="nav-link text-light me-3">All Drivers</a>
                <a href="/drivers" class="nav-link text-light me-3">API Drivers</a>
                <a href="/drivers/new" class="nav-link text-light me-3">Add A New Driver</a>
                <a href="/logout" class="nav-link text-danger me-3">LOG OUT</a>
            <% } else { %>
                <a href="/auth/google" class="login">LOG IN&nbsp;
                </a>
            <% } %>
        </div>
    </div>
</nav>
<hr>

<h3><%= title %></h3>